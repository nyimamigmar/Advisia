name: Tägliche SHAB-Prüfung Kanton Aargau

on:
  schedule:
    # 07:15 UTC = 08:15 MEZ (Winter) / 09:15 MESZ (Sommer)
    - cron: "15 7 * * 1-5"
  workflow_dispatch:
    inputs:
      reset_state:
        description: "Statusdatei zurücksetzen (neuer Erstlauf)"
        required: false
        default: "false"
        type: boolean
  push:
    branches:
      - main
    paths:
      - 'src/**'

permissions:
  contents: write

jobs:
  check-new-companies:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Statusdatei zurücksetzen (falls angefordert)
        if: ${{ inputs.reset_state == 'true' }}
        run: rm -f data/seen_companies.json

      - name: Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Abhängigkeiten installieren
        run: pip install -r requirements.txt

      - name: SHAB-Screening durchführen & Tagesbericht senden
        run: python src/main.py
        env:
          SENDER_COMPANY:  ${{ vars.SENDER_COMPANY }}
          SENDER_NAME:     ${{ vars.SENDER_NAME }}
          SENDER_ADDRESS:  ${{ vars.SENDER_ADDRESS }}
          SENDER_ZIP_CITY: ${{ vars.SENDER_ZIP_CITY }}
          SENDER_PHONE:    ${{ vars.SENDER_PHONE }}
          SENDER_EMAIL:    ${{ vars.SENDER_EMAIL }}
          SENDER_WEBSITE:  ${{ vars.SENDER_WEBSITE }}
          RESEND_API_KEY:  ${{ secrets.RESEND_API_KEY }}

      - name: Generierte Briefe als Artefakt speichern
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: briefe-${{ github.run_id }}
          path: letters/
          retention-days: 90
          if-no-files-found: ignore

      - name: Statusdatei committen und pushen
        run: |
          git config user.name  "SHAB Bot"
          git config user.email "shab-bot@users.noreply.github.com"
          git add data/seen_companies.json
          git diff --staged --quiet || \
            git commit -m "SHAB-Check $(date '+%Y-%m-%d'): Statusdatei aktualisiert"
          git push origin ${{ github.ref_name }}
